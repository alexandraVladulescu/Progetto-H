package data;

/**
 * Questa enum può assumere due soli valori: CREATE (stiamo creando una nuova comanda)
 * , MODIFY (stiamo modificando una comanda già esistente).
 * @author Markenos
 */
public enum CurrentComandaManagerModality {
    CREATE, MODIFY;
}
